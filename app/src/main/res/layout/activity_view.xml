<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="?colorSurfaceVariant"
    android:fitsSystemWindows="true"
    android:orientation="vertical"
    tools:context=".ViewActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">
            <ImageView
                android:id="@+id/photo"
                android:layout_width="match_parent"
                android:layout_height="20dp"
                android:contentDescription="@string/photo_description"
                android:scaleType="centerInside"
                android:src="@drawable/ic_launcher_monochrome" />
            <ImageButton
                android:id="@+id/photo_reload"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:visibility="gone"
                android:backgroundTint="?attr/colorSecondary"
                android:tint="@android:color/white"
                android:layout_alignParentStart="true"
                android:src="@drawable/ic_reload_24dp"
                android:onClick="reload"/>
            <ImageButton
                android:id="@+id/photo_share"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:visibility="gone"
                android:backgroundTint="?attr/colorSecondary"
                android:tint="@android:color/white"
                android:layout_toEndOf="@id/photo_reload"
                android:src="@drawable/ic_share_24dp"
                android:onClick="share"/>
            <ImageButton
                android:id="@+id/photo_ebird"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:visibility="gone"
                android:backgroundTint="?attr/colorSecondary"
                android:tint="@android:color/white"
                android:layout_below="@id/photo_reload"
                android:src="@drawable/ic_info_24dp"
                android:onClick="ebird"/>
            <TextView
                android:id="@+id/photo_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:visibility="gone"
                android:layout_alignParentTop="true"
                android:layout_alignParentEnd="true"
                android:background="?colorSurfaceVariant"
                android:paddingHorizontal="3dp"
                android:layout_marginEnd="10dp"
                android:textStyle="bold"
                android:textIsSelectable="true"
                android:textColor="?colorPrimary"
                android:textSize="14sp" />
            <TextView
                android:id="@+id/photo_latinname"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:visibility="gone"
                android:layout_below="@id/photo_name"
                android:layout_alignParentEnd="true"
                android:background="?colorSurfaceVariant"
                android:paddingHorizontal="3dp"
                android:layout_marginEnd="10dp"
                android:textStyle="italic"
                android:textIsSelectable="true"
                android:textColor="?colorPrimary"
                android:textSize="10sp" />
        </RelativeLayout>
        <!-- The shape of what was heard, under the bird it was. Height is set in code so
             it keeps a fixed slice of the panel; gone rather than invisible because unlike
             the photograph this one is absent for most rows, and a permanent empty band
             would cost the log a row to say nothing. -->
        <ImageView
            android:id="@+id/spectrogram"
            android:layout_width="match_parent"
            android:layout_height="20dp"
            android:visibility="gone"
            android:contentDescription="@string/show_spectrogram"
            android:scaleType="fitXY" />
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/recyclerObservations"
                android:layout_width="match_parent"
                android:layout_height="match_parent"/>

        </LinearLayout>

    </LinearLayout>
    <androidx.compose.ui.platform.ComposeView
        android:id="@+id/destinationRow"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />


</LinearLayout>